import { game } from './functions.js';

describe('game', () => {
  test('should be ... when argument is ...', () => {
    //Arrange
    const x = ...;
    const expected =...;
    //Act
    const r = game();
    //Assert
    expect(r).toBe(expected);
  });
    test('should be ... when argument is ...', () => {
    //Arrange
    const x = ...;
    const expected =...;
    //Act
    const r = game();
    //Assert
    expect(r).toBe(expected);
  });
    test('should be ... when argument is ...', () => {
    //Arrange
    const x = ...;
    const expected =...;
    //Act
    const r = game();
    //Assert
    expect(r).toBe(expected);
  });
    test('should be ... when argument is ...', () => {
    //Arrange
    const x = ...;
    const expected =...;
    //Act
    const r = game();
    //Assert
    expect(r).toBe(expected);
  });
})
